<x-layouts.app title="Review RTLH"><div class="pagehead"><div class="kicker">ADMIN REVIEW</div><h1>Menunggu Review</h1></div><div class="card tablewrap"><table><thead><tr><th>Kode</th><th>Wilayah</th><th>Tahun</th><th>Surveyor</th><th>Aksi</th></tr></thead><tbody>@forelse($houses as $h)<tr><td>{{ $h->house_code }}</td><td>{{ $h->region?->name }}</td><td>{{ $h->survey_year }}</td><td>{{ $h->created_by }}</td><td><a href="{{ route('admin.rtlh.show',$h) }}">Detail</a> <form class="inline" method="post" action="{{ route('admin.review.approve',$h) }}">@csrf<button class="btn small">Approve</button></form><form class="inline" method="post" action="{{ route('admin.review.revision',$h) }}">@csrf<button class="btn small danger">Revisi</button></form></td></tr>@empty<tr><td colspan="5">Tidak ada submission.</td></tr>@endforelse</tbody></table></div>{{ $houses->links() }}</x-layouts.app>
